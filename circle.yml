machine:
  xcode:
    version: 7.3
  environment:
    BUILD_TYPE: "Debug"
    XCODE_SCHEME: "no"
    XCODE_WORKSPACE: "no"
    CCACHE_COMPRESS: 1

dependencies:
  pre:
    - make

test:
  override:
    - echo "success"
